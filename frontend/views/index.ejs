<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/style.css">
</head>
<body class="red center">
    <section class='login-container' >
            <div class='display-3'>Welcome to RecipeShare!</div>
        <section class='py-2 d-flex justify-content-center'>
            <form method="POST" action="/login">
                <input class='mt-4 mb-1 form-control' type="email"  placeholder="Email" name="email" required/>
                <% if (error1) { %>
                <span class="red-font">
                    <%= error1 %>
                </span>
                <% } %>
                
                <input class='mt-3 mb-1 form-control' type="password"  placeholder="Password" name="password" required/>
                <% if (error2) { %>
                <span class="red-font">
                    <%= error2 %>
                </span>
                <% } %>
                <section class='py-2'>
                    <button type="submit" class="login-button">Log In</button>
                    <a href="/register" class="m-3" id="register-link">Don't have an account?</a>
                </section>
            </form>        
        </section>    
    </section>
</body>
</html>